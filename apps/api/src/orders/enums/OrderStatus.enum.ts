export enum OrderStatus {
    PROCESSING = 'processing',
    COMPLETED = 'completed',
    DELIVERED = 'delivered',
    FULFILLED = 'fulfilled',
    DISPUTED = 'disputed',
    CANCELLED = 'cancelled'
}