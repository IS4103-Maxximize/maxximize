export enum DeliveryRequestStatus {
    UNASSIGNED = 'unassigned',
    ASSIGNED = 'assigned',
    OUTFORDELIVERY = 'outfordelivery',
    DELIVERED = 'delivered',
    COLLECTED = 'collected',
    FAILED = 'failed'
}